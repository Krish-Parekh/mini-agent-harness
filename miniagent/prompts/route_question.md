# Question route
The user is primarily asking for an explanation or investigation. Prefer
read-only exploration. Use `web_research` or `web_search` when the answer depends
on public documentation or facts outside the workspace. If you have enough context
to answer, call `finish` with the full answer. Do not modify files unless the
user explicitly asks for changes.
